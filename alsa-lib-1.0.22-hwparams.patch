--- src/pcm/pcm.c.orig	2011-08-14 16:08:55.718146254 +0200
+++ src/pcm/pcm.c	2011-08-14 16:10:47.668320507 +0200
@@ -2180,7 +2180,7 @@
 			}
 		} else {
 			char *str = getenv("LIBASOUND_COMPAT");
-			if (str && *str)
+			if (str && *str && *str != 'n' && *str != 'N' && *str != '0')
 				(*pcmp)->compat = 1;
 		}
 		err = snd_config_search(pcm_root, "defaults.pcm.minperiodtime", &tmp);
